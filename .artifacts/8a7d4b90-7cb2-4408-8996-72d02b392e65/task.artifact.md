# Tasks

- `[/]` Session Management (Day 4)
    - `[ ]` Create `data` package and `SessionManager.kt`
    - `[ ]` Update `MainActivity.kt` to check session
    - `[ ]` Update `NavGraph.kt` for dynamic start destination
    - `[ ]` Update `LoginScreen.kt` to save session
    - `[ ]` Update `WelcomeScreen.kt` to save session (if needed)
